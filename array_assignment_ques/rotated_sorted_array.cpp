#include <iostream>
using namespace std;

int main()
{
    int arr[] = {4, 5, 6, 7, 0, 1, 2};
    int n = sizeof(arr) / sizeof(int);
    int left = 0;
    int right = n - 1;

    while (left <= right)
    {
        int mid = (left + right) / 2;
        if(mid== arr[key])
        {
            console.log("key is found");
        }
        else if(arr[key]>mid)
        {
            
        }
    }
}